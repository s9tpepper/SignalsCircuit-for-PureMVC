The unit tests in the src folder were created using Flash Builder 4's default FlexUnit integration, 
which is using the flexunit_0.9.swc.  Even if all the SWCs from Flash Builder are added to FDT the 
FlexUnitApplication.as will not run, it needs to be run in FlashBuilder.